name: iOS Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v2

    - name: Select Xcode
      run: sudo xcode-select -s /Applications/Xcode.app

    - name: Setup iOS SDK
      run: |
        SDKROOT=$(xcrun --sdk iphoneos --show-sdk-path)
        echo "SDKROOT=$SDKROOT" >> $GITHUB_ENV

    - name: List directory contents
      run: ls -la

    - name: Compile main.c
      run: |
        xcrun -sdk iphoneos clang -c main.c -o main.o -arch arm64 -mios-version-min=14.0

    - name: Assemble main.s
      run: |
        xcrun -sdk iphoneos clang -c main.s -o main_asm.o -arch arm64 -mios-version-min=14.0

    - name: Link object files
      run: |
        xcrun -sdk iphoneos clang main.o main_asm.o -o main -arch arm64 -mios-version-min=14.0 -framework Foundation -framework CoreFoundation

    - name: Upload artifacts
      uses: actions/upload-artifact@v2
      with:
        name: ios-arm64-binary
        path: main
